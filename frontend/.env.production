# Production API URL — points to your Hostinger VPS / domain
# Replace with your actual domain (with https if SSL is configured)
# or your VPS IP (http://YOUR_VPS_IP) if no domain yet.
VITE_API_URL=https://yourdomain.com
